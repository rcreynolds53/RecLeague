@model RecLeagueBlog.Models.LoginModel
@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>
    @Html.ValidationSummary(true)
    @using (Html.BeginForm())
    {
        @Html.EditorForModel()
        <div class="form-group">
            <div class="col-xs-12">
                <button type="submit" class="btn btn-default">Login</button>
            </div>
        </div>
    }
